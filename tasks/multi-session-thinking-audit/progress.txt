# Ralph Progress

Requirement: tasks/multi-session-thinking-audit
Started: 2026-05-21 20:08
Source: prd.json

## Codebase Patterns

- 后端运行状态以 `task_id`/`run_id` 为边界，`storage.start_run()`、`TaskRunner.start_background()`、`append_event()` 和终端事件需要保持同一个 `run_id`。
- 前端字段转换集中在 `frontend/app/task-state.ts`，默认渲染应使用简洁 live metadata，完整运行证据通过 `rawRecord`、expanded diagnostics 和 JSONL copy 暴露。
- `reasoning_content` 是 run 级诊断证据，只能保存在当前 run 的事件日志与展开诊断中，不能进入普通 messages、conversation context、长期记忆、Qdrant payload 或默认 artifact。
- 行为变更必须同步测试、实际浏览器 E2E、截图证据和 `asset/deepagents_platform_knowledge_pack.md`。
- OpenAI-compatible provider payload 表征测试可以先用 `langchain_openai.chat_models.base._convert_message_to_dict()` 复用 LangChain 既有序列化，再由本地 adapter 精确补回 provider 专属字段。
- DeepSeek message adapter 需要和 `ChatDeepSeek._get_request_payload()` 保持一致：assistant content list 要折叠为字符串、tool content list 要 JSON 化，再只对当前 assistant tool-call turn 回放 `reasoning_content`。
- `app/models/provider.py#create_model()` 是切换 provider wrapper 的稳定入口：non-thinking ID 保持 `ChatDeepSeek`，thinking-enabled ID 才切到 `DeepSeekThinkingChatModel`，这样上层调用方不用感知 adapter。
- `backend/app/streaming/v2_adapter.py` 的 `thinking_chunk` 既是完整 reasoning 载体，也是 tool-call 诊断挂点：如果同一 provider turn 带了工具调用，优先把 `tool_call_id`/`tool_call_ids`/`tool_calls` 放进 thinking payload，再由 `event_converter.py` 保持稳定的 `payload.live.display_text`，不要把这些诊断字段塞进 collapsed 文案。
- `backend/app/streaming/event_converter.py` 必须把 `assistant_thinking_delta` 的完整 reasoning 放在 `payload.content`，同时让顶层 `message` 和 `payload.live.display_text` 都保持稳定状态文案；这样即使旧消费方回退到 `message`，也不会把 reasoning 泄露到默认 UI。
- `backend/app/api/tasks.py#send_message()` 的同 task 互斥是双层保护：先检查 `runner.is_running(task_id)` 立即返回 409，再由 `storage.start_run(... expected_statuses=...)` 兜底阻止非允许状态下创建新 run。

## 2026-05-21 20:45 - US-001
- 实现了 DeepSeek thinking/tool-call request message 的表征测试，并新增一个未接线的 helper 作为稳定断言目标，锁定 assistant/tool provider payload 结构。
- 更改的文件：`backend/app/models/deepseek_thinking.py`、`backend/tests/unit/models/test_deepseek_thinking_messages.py`、`tasks/multi-session-thinking-audit/prd.json`
- **未来迭代的学习：**
  - 发现的 patterns（例如，"这个 codebase 使用 X 来做 Y"）
  - OpenAI-compatible provider message 序列化已经由 LangChain 处理好基础 role/content/tool fields，DeepSeek thinking 只需要在本地 adapter 上补 provider-specific `reasoning_content`。
  - 遇到的陷阱（例如，"更改 W 时不要忘记更新 Z"）
  - `ChatDeepSeek` 当前不会把 `AIMessage.additional_kwargs["reasoning_content"]` 自动写回 outbound payload；后续接线必须走 wrapper 或 request payload hook。
  - 有用的上下文（例如，"评估面板在 component X 中"）
  - `ToolMessage` 的 provider payload 已经稳定保留 `tool_call_id`，这部分不需要额外补丁，只要别在 adapter 里重写 tool message 结构。
---

## 2026-05-21 21:00 - US-002
- 实现了可复用的 DeepSeek thinking adapter/wrapper，使 thinking tool-call turn 在请求 payload 中保留 `reasoning_content`，同时保持与 `ChatDeepSeek` 原生 content 归一化逻辑一致，并补了缺失 reasoning 不伪造的回归测试。
- 更改的文件：`backend/app/models/deepseek_thinking.py`、`backend/tests/unit/models/test_deepseek_thinking_messages.py`、`asset/deepagents_platform_knowledge_pack.md`、`tasks/multi-session-thinking-audit/prd.json`
- **未来迭代的学习：**
  - 发现的 patterns（例如，"这个 codebase 使用 X 来做 Y"）
  - 如果要包 `ChatDeepSeek`，最稳妥的切点是重写 `_get_request_payload()` 后复用 `self._convert_input(...).to_messages()`，这样不会丢掉 LangChain 默认参数和 DeepSeek 自带 payload 结构。
  - 遇到的陷阱（例如，"更改 W 时不要忘记更新 Z"）
  - 仅靠 `_convert_message_to_dict()` 还不够；`ChatDeepSeek` 额外要求 assistant list content 折叠为字符串、tool list content 序列化成 JSON，不对齐这里会让 adapter 与真实 provider 行为漂移。
  - 有用的上下文（例如，"评估面板在 component X 中"）
  - 这一轮只实现 adapter 层本身，`create_model()` 的入口接线仍留给 US-003，避免把 story 边界混在一起。
---

## 2026-05-21 21:14 - US-003
- 在 `create_model()` 入口按 `thinking_mode` 选择 `DeepSeekThinkingChatModel` 或 `ChatDeepSeek`，保持原有 provider 初始化参数不变，并新增 payload 回归测试确认只有 thinking 模型会回放 `reasoning_content`；同时通过 `uv run pytest tests/unit/models/test_provider.py tests/unit/models/test_deepseek_thinking_messages.py -q`、`uv run ruff check .`、`uv run mypy app tests` 和 `git diff --check`。
- 更改的文件：`backend/app/models/provider.py`、`backend/tests/unit/models/test_provider.py`、`asset/deepagents_platform_knowledge_pack.md`、`tasks/multi-session-thinking-audit/prd.json`
- **未来迭代的学习：**
  - 发现的 patterns（例如，"这个 codebase 使用 X 来做 Y"）
  - provider 入口适合做“registry model ID -> ChatModel 类”的切换；这样 `agent/factory.py`、`memory.py` 和 `task_titles.py` 继续只依赖 `create_model()`，不用知道 thinking wrapper 的存在。
  - 遇到的陷阱（例如，"更改 W 时不要忘记更新 Z"）
  - `create_model()` 的返回类型是 `BaseChatModel`；测试如果要探测 `_get_request_payload()` 这类 provider 私有方法，需要在测试里显式 `cast`，否则 `mypy` 会报属性不存在。
  - 有用的上下文（例如，"评估面板在 component X 中"）
  - 这一轮只改了后端 provider 接线，没有前端 UI surface 变化；真实 outbound payload 行为由 `backend/tests/unit/models/test_provider.py` 直接断言。
---

## 2026-05-21 21:32 - US-004
- 实现了 `thinking_chunk` 的完整诊断挂载：当 provider reasoning 与 tool-call 同 turn 到达时，流式适配层会把 `tool_call_id`、`tool_call_ids` 和标准化 `tool_calls` 一起带入 thinking 事件；同时保持 subgraph reasoning 仍是诊断内容，不会混进 root answer delta。
- 更改的文件：`backend/app/streaming/v2_adapter.py`、`backend/app/streaming/event_converter.py`、`backend/tests/unit/streaming/test_v2_adapter.py`、`backend/tests/unit/streaming/test_event_converter.py`、`asset/deepagents_platform_knowledge_pack.md`、`tasks/multi-session-thinking-audit/prd.json`
- 验证：`cd backend && uv run pytest -q`、`cd backend && uv run ruff check .`、`cd backend && uv run mypy app tests`、`git diff --check` 全部通过；未运行浏览器 E2E，因为本轮只改后端流式诊断事件结构，没有新增或修改前端交互入口。
- **未来迭代的学习：**
  - 发现的 patterns（例如，"这个 codebase 使用 X 来做 Y"）
  - `thinking_chunk` 的诊断扩展最好复用 `tool_call` 已有的标准化结构，这样前端后续读取 run-level diagnostics 时不用再维护第二套工具调用 schema。
  - 遇到的陷阱（例如，"更改 W 时不要忘记更新 Z"）
  - 只在 `v2_adapter.py` 增字段还不够；`event_converter.py` 对 `assistant_thinking_delta` 有固定 payload 白名单，不同步透传就会在持久化事件里丢掉 tool-call 关联信息。
  - 有用的上下文（例如，"评估面板在 component X 中"）
  - 现有 root/subgraph 隔离已经由 namespace 判定和 `message_chunk` 过滤保障；补 thinking 诊断时只要保留 `is_subgraph` 并避免发出 subgraph `message_chunk`，就不会污染最终答案流。
---

## 2026-05-21 22:06 - US-005
- 收紧了 `assistant_thinking_delta` 的默认输出边界：完整 reasoning 继续保留在 `payload.content` 和展开诊断里，但顶层 `message` 改为稳定的 `AI正在思考...`，避免旧消费路径把 reasoning 当默认文案展示；同时补了 canary 回归测试验证 `live.display_text` 也不会泄露 reasoning。
- 更改的文件：`backend/app/streaming/event_converter.py`、`backend/tests/unit/streaming/test_event_converter.py`、`asset/deepagents_platform_knowledge_pack.md`、`tasks/multi-session-thinking-audit/prd.json`
- 验证：`cd backend && uv run pytest tests/unit/streaming/test_event_converter.py -q`、`cd backend && uv run pytest -q`、`cd backend && uv run ruff check .`、`cd backend && uv run mypy app tests`、`git diff --check` 全部通过；浏览器 smoke 复用了 `http://127.0.0.1:3001` 并临时启动 `http://127.0.0.1:8001`，确认 thinking 会话 collapsed 进度仅显示 `AI正在思考`，展开 JSON 仍包含完整 reasoning，截图与快照保存在 `tasks/multi-session-thinking-audit/screenshots/us005-thinking-collapsed-expanded.png` 和 `tasks/multi-session-thinking-audit/logs/us005-browser-*.md|.log`。
- **未来迭代的学习：**
  - 发现的 patterns（例如，"这个 codebase 使用 X 来做 Y"）
  - `assistant_thinking_delta` 的安全边界最好在后端事件层就收紧，而不是依赖前端永远优先读取 `payload.live.display_text`；只要让 `payload.content` 承担完整诊断文本，legacy `message` 回退路径也能保持安全。
  - 遇到的陷阱（例如，"更改 W 时不要忘记更新 Z"）
  - 只看单元测试会误判“默认文案安全”已经成立；真实浏览器里 progress row 的 copy/expand/fallback 路径仍会接触顶层 `message`，所以这类事件边界变更最好补一次最小 smoke。
  - 有用的上下文（例如，"评估面板在 component X 中"）
  - 本地前端在 `3001` 可单独运行，但浏览器 smoke 依旧依赖真实后端 `8001` 才能创建新 run；这次通过新建 `DeepSeek V4 Flash Thinking` 会话直接验证了 collapsed 与 expanded diagnostics 的分层表现。
---

## 2026-05-21 22:25 - US-006
- 新增后端并发与互斥回归测试，验证两个不同 task 可以同时保持 `running`，同一 task 在运行中再次发送会返回 `409` 且不会创建第二个 run，并断言 A task 失败时只会把 A 标记为 failed，不会改变 B 的 `active_run_id` 或运行状态。
- 更改的文件：`backend/tests/unit/runner/test_concurrency_and_thinking_audit.py`、`backend/tests/unit/api/test_tasks.py`、`tasks/multi-session-thinking-audit/prd.json`
- 验证：`cd backend && uv run pytest tests/unit/runner/test_concurrency_and_thinking_audit.py tests/unit/api/test_tasks.py -q`、`cd backend && uv run pytest -q`、`cd backend && uv run ruff check .`、`cd backend && uv run mypy app tests`、`git diff --check` 全部通过；未运行浏览器 E2E，因为本轮只补后端并发/互斥测试，没有新增前端交互入口或运行时行为改动。
- **未来迭代的学习：**
  - 发现的 patterns（例如，"这个 codebase 使用 X 来做 Y"）
  - `send_message()` 的同 task 互斥要同时看进程内 runner 和持久化状态：前者负责即时 `409`，后者通过 `expected_statuses` 保证不会在非允许状态下新建 run。
  - 遇到的陷阱（例如，"更改 W 时不要忘记更新 Z"）
  - 并发回归测试不要靠 `sleep` 猜时序；用 monkeypatch 的 fake `runner.start()` 配合 `asyncio.Event` 才能稳定验证“双 task 并行、单 task 互斥”。
  - 有用的上下文（例如，"评估面板在 component X 中"）
  - `TaskRunner._active_runs` 只按 `task_id` 注册当前进程内活跃任务，所以 A/B 隔离回归只需要断言各自 `status`、`active_run_id` 和终端事件，不需要引入额外的全局锁测试夹具。
---
